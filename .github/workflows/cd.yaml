name: Continuous Deployment (CD)

on:
  push:
    branches: [ main, master ]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ ISPRAVNA WIF AUTENTIFIKACIJA
      - name: Authenticate to GCP (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: github-actions-sa@student-0036540224-project.iam.gserviceaccount.com

      # ✅ OBAVEZNO
      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Verify identity
        run: gcloud auth list

      - name: Configure Docker
        run: gcloud auth configure-docker europe-west1-docker.pkg.dev

      - name: Build consumer
        run: |
          docker build -t europe-west1-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/jsonplaceholder-images/consumer:latest ./consumer

      - name: Build producer
        run: |
          docker build -t europe-west1-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/jsonplaceholder-images/producer:latest ./producer

      - name: Push consumer
        run: docker push europe-west1-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/jsonplaceholder-images/consumer:latest

      - name: Push producer
        run: docker push europe-west1-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/jsonplaceholder-images/producer:latest

      # ✅ CLOUD RUN SERVICE
      - name: Deploy consumer
        run: |
          gcloud run deploy consumer \
            --image=europe-west1-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/jsonplaceholder-images/consumer:latest \
            --region=europe-west1 \
            --allow-unauthenticated \
            --service-account=cr-deploy2-sa@student-0036540224-project.iam.gserviceaccount.com

      # ✅ CLOUD RUN JOB2
      - name: Deploy producer job
        run: |
          gcloud run jobs deploy producer \
            --image=europe-west1-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/jsonplaceholder-images/producer:latest \
            --region=europe-west1 \
            --service-account=cr-deploy2-sa@student-0036540224-project.iam.gserviceaccount.com

      - name: Run producer job
        run: gcloud run jobs execute producer --region=europe-west1
